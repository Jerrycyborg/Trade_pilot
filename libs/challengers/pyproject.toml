[project]
name = "challengers"
version = "0.1.0"
description = "L3: bounded parameter proposals, evaluated with campaign-wide trial counting."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/challengers"]

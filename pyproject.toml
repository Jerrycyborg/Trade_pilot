[project]
name = "trading-stack"
version = "0.1.0"
description = "Milestone 1 bootstrap for a production-minded AI trading stack."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "contracts",
  "market_data",
  "brokers",
  "execution-service",
  "portfolio-service",
  "policy-service",
  "strategy-service",
  "research-service",
]

[dependency-groups]
dev = [
  "httpx>=0.28.0",
  "pytest>=8.3.0",
  "ruff>=0.11.0",
]

[tool.uv.workspace]
members = [
  "libs/contracts",
  "libs/market_data",
  "libs/brokers",
  "services/execution-service",
  "services/portfolio-service",
  "services/policy-service",
  "services/strategy-service",
  "services/research-service",
]

[tool.uv.sources]
contracts = { workspace = true }
market_data = { workspace = true }
brokers = { workspace = true }
execution-service = { workspace = true }
portfolio-service = { workspace = true }
policy-service = { workspace = true }
strategy-service = { workspace = true }
research-service = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]
